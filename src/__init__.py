"""
Top-level namespace is now provided by the `moria` package.

This module is intentionally left minimal; import from `moria` instead:

    import moria
    from moria import matchup_files, extract_psf_1
"""
